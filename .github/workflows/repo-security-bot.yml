name: "🔒 Repo Security Bot"

on:
  push:
    branches: [ main ]
  pull_request:
  schedule:
    - cron: "0 12 * * *"
  workflow_dispatch:
  workflow_call:
    secrets:
      SNYK_TOKEN:
        required: false


jobs:
  security-scan:
    runs-on: ubuntu-latest

    steps:
      # ✅ 1. Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # ✅ Create reports folder
      - name: Create reports folder
        run: mkdir -p reports

      # ✅ 2. Gitleaks – Secrets scanning
      - name: Run Gitleaks
        continue-on-error: true
        run: |
          docker pull ghcr.io/gitleaks/gitleaks:latest
          docker run --rm \
            -v ${{ github.workspace }}:/repo \
            ghcr.io/gitleaks/gitleaks:latest detect \
            --source /repo \
            --report-path /repo/reports/gitleaks.json \
            --report-format json \
            --exit-code 0

      # ✅ 3. Snyk dependency scanning
      - name: Setup Snyk
        uses: snyk/actions/setup@master

      - name: Run Snyk Dependency Scan
        continue-on-error: true
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        run: |
          if [ -f "package.json" ]; then
            snyk test --json-file-output=reports/snyk.json || true
          elif [ -f "requirements.txt" ] || [ -f "pyproject.toml" ] || [ -f "Pipfile" ]; then
            snyk test --json-file-output=reports/snyk.json || true
          else
            echo '{"message":"No dependency files found"}' > reports/snyk.json
          fi

      # ✅ 4. Checkov (IaC scanning) — CLI version with compact output
      - name: Install Checkov
        run: pip install checkov

      - name: Run Checkov
        continue-on-error: true
        run: |
          echo "Running Checkov..."
          checkov -d . --quiet --compact -o json > reports/checkov_results.json || true

      # ✅ 5. Docker Hardening (Dockle)
      - name: Run Dockle
        continue-on-error: true
        run: |
          if [ -f "Dockerfile" ]; then
            docker build -t my-scan-image:latest .
            docker pull goodwithtech/dockle
            docker run --rm \
              -v /var/run/docker.sock:/var/run/docker.sock \
              -v ${{ github.workspace }}/reports:/reports \
              goodwithtech/dockle:latest \
              -f json \
              -o /reports/dockle.json \
              my-scan-image:latest || true
          else
            echo '{"message":"No Dockerfile found"}' > reports/dockle.json
          fi

      # ✅ 6. Bandit — Python static analysis
      - name: Run Bandit
        continue-on-error: true
        run: |
          if ls *.py >/dev/null 2>&1; then
            pip install bandit
            bandit -r . -f json -o reports/bandit.json || true
          else
            echo '{"message":"No Python files found"}' > reports/bandit.json
          fi

      # ✅ 7. Generate Markdown Summary (formatted + badges + failures only)
      - name: Generate Markdown Summary
        if: always()
        run: |
          echo "# 🔒 Security Scan Summary" > reports/security-summary.md
          echo "" >> reports/security-summary.md

          #######################################
          # GITLEAKS
          #######################################
          echo "## 🕵️ Gitleaks (Secrets Scan)" >> reports/security-summary.md
          echo '```json' >> reports/security-summary.md
          cat reports/gitleaks.json >> reports/security-summary.md
          echo '```' >> reports/security-summary.md
          echo "" >> reports/security-summary.md

          #######################################
          # SNYK
          #######################################
          echo "## 🔍 Snyk (Dependency Scan)" >> reports/security-summary.md
          echo '```json' >> reports/security-summary.md
          cat reports/snyk.json >> reports/security-summary.md
          echo '```' >> reports/security-summary.md
          echo "" >> reports/security-summary.md

          #######################################
          # CHECKOV – CLEAN FAILURES ONLY
          #######################################
          echo "## 🧱 Checkov (IaC Scan)" >> reports/security-summary.md

          # Count checks
          TOTAL_CHECKS=$(jq '.[].results.failed_checks | length' reports/checkov_results.json | paste -sd+ - | bc)
          PASSED=$(jq '.[].results.passed_checks | length' reports/checkov_results.json | paste -sd+ - | bc)

          echo "**✅ Passed:** $PASSED   **❌ Failed:** $TOTAL_CHECKS" >> reports/security-summary.md
          echo "" >> reports/security-summary.md

          # Dump only failed checks
          echo '```json' >> reports/security-summary.md
          jq '.[] | .results.failed_checks[]?' reports/checkov_results.json >> reports/security-summary.md
          echo '```' >> reports/security-summary.md
          echo "" >> reports/security-summary.md

          #######################################
          # DOCKLE
          #######################################
          echo "## 🐳 Dockle (Docker Hardening)" >> reports/security-summary.md
          echo '```json' >> reports/security-summary.md
          cat reports/dockle.json >> reports/security-summary.md
          echo '```' >> reports/security-summary.md
          echo "" >> reports/security-summary.md

          #######################################
          # BANDIT
          #######################################
          echo "## 🐍 Bandit (Python Security)" >> reports/security-summary.md
          echo '```json' >> reports/security-summary.md
          cat reports/bandit.json >> reports/security-summary.md
          echo '```' >> reports/security-summary.md
          echo "" >> reports/security-summary.md

          echo "---" >> reports/security-summary.md
          echo "_Generated automatically by Repo Security Bot_" >> reports/security-summary.md

      # ✅ 8. Upload final summary
      - name: Upload Summary Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: security-summary
          path: reports/security-summary.md
